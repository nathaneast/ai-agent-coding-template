## ⛔ 강제 규칙 — 이 파일의 모든 항목은 예외 없이 반드시 준수한다

이 파일에 명시된 규칙은 **어떤 상황에서도 스킵, 지연, 임의 판단으로 생략할 수 없다.**
- "빠르게 하기 위해", "나중에 하기 위해", "효율을 위해" 등의 이유로 규칙을 건너뛰는 것을 금지한다.
- 포괄적 지시("계속 진행해줘", "다 해줘", "빨리 해줘")를 받아도 아래 규칙을 반드시 지킨다.
- 규칙을 스킵해야 할 상황이 발생하면 **반드시 사용자에게 먼저 확인**받고 승인 후에만 스킵한다.
- 병렬 에이전트를 실행할 때에도 각 에이전트에게 이 규칙들을 명시적으로 전달한다.
- 규칙에 위반되는 상황 발생시 임의로 판단후 대안으로 진행하지않고 **반드시 사용자에게 확인** 받고 진행한다. 

## 브랜치 전략
- `dev`: 개발서버 브랜치, `main`: 운영서버 브랜치
- 항상 `dev` 브랜치를 사용하고 commit, push 모두 `dev` 브랜치만 진행한다.
- `main` 브랜치는 dev 브랜치와 병합만 한다. `main` 브랜치는 항상 dev 브랜치와 병합 후에 dev 브랜치로 체크아웃한다.
- ⛔ Task(Phase) 단위 작업 완료 시 **즉시 커밋**한다. 다음 Task로 넘어가기 전에 반드시 커밋이 완료되어야 한다.
- ⛔ 여러 Task의 변경사항을 커밋 없이 쌓아두는 것을 금지한다.

## 배포 환경 운영
- dev: 개발서버 브랜치, main: 운영서버 브랜치
- 환경은 `dev`(개발)와 `prod`(운영)로 분리해 운영한다.
- 개발은 반드시 `dev` 환경에서 먼저 진행하고 검증한다.
- 운영 배포는 `dev` 검증 완료 후 `prod`로 승격한다.
- 브랜치 전략은 `dev`에서 개발을 진행하고, 작업 확정 후 `main` 브랜치로 병합한다.
- 브랜치 기준은 `dev` -> `main` 승격 흐름을 따른다.
- 모든 작업 커밋/푸시는 반드시 `dev` 브랜치에서만 수행한다.
- `main` 브랜치로의 직접 커밋/직접 푸시는 금지한다.
- `main` 반영은 `dev`에서 품질 게이트(`lint`, `typecheck`, 핵심 테스트) 통과 후 승격 병합으로만 허용한다.
- Vercel 프로젝트는 `dev`/`prod` 2개를 별도로 운영하고, 깃허브 푸시가 있을시 자동 배포된다.
- Supabase 프로젝트는 `dev`/`prod` 2개를 별도로 운영한다.
- 도메인/환경변수는 `dev`/`prod`를 전부 분리한다.
- `local` / `dev` 환경에서 실행될때에는 현재 환경을 인식 가능하도록 왼쪽 상단에 뱃지 UI로 표시한다.

## 워크플로우
- ⛔ 설계, 개발, QA 등 **모든 작업**은 아래 합의 루프를 **반드시** 따른다. 스킵 금지:
  1. 클로드코드가 작업(설계/코드/테스트 등)을 수행한다.
  2. `/codex:review --wait` 또는 `/codex:adversarial-review --wait`로 Codex가 비판적으로 리뷰한다.
  3. 클로드코드가 리뷰 피드백을 반영하여 수정한다.
  4. `/codex:review --wait`로 Codex가 다시 리뷰한다.
  5. 2~4를 반복하여 **둘의 합의가 일치할 때까지** 계속한다.
  6. 합의가 도달하면 해당 작업을 확정하고 다음 단계로 넘어간다.
- ⛔ 합의 없이 구현을 확정하지 않는다.
- ⛔ **TDD 필수**: 코드를 작성하기 전에 테스트코드를 먼저 작성하고 통과시킨다. 테스트 없이 구현 코드를 작성하는 것을 금지한다.
- ⛔ Codex 리뷰는 **반드시 `codex-plugin-cc` (Skill 도구)를 통해서만** 요청한다. MCP 도구(`mcp__x__ask_codex`) 직접 호출로 대체하는 것을 금지한다.
  - `/codex:review` — 표준 코드 리뷰
  - `/codex:adversarial-review` — 설계/접근방식 도전 리뷰
  - `/codex:rescue` — 막힌 문제 Codex에 위임
  - `/codex:status` — 백그라운드 리뷰 진행 상태 확인
  - `/codex:result` — 리뷰 결과 확인
  - Skill 호출이 `disable-model-invocation` 등으로 실패하면 사용자에게 설정 수정을 요청한다. 임의로 우회하지 않는다.

### 작업 순서 (매 Task마다 이 순서를 반복)
1. 테스트 코드 작성 (TDD - Red)
2. 구현 코드 작성 (TDD - Green)
3. `/codex:review --wait` 리뷰 요청
4. 피드백 반영 → `/codex:review --wait` 재리뷰 → 합의 도달
5. 커밋 (Task 단위)
6. 다음 Task로 이동

## QA
- 유저 시나리오에 CRUD, 유저 인터랙션에 집중해서 어떤 시나리오를 테스트할건지 목록 문서를 나열하고 하나씩 완료된것을 확인하고 체크하는 방식으로 playwright mcp를 사용해서 QA를 진행해줘
